import React from "react";
import {
  BrowserRouter as Router,
  Switch,
  Route,
  Link,
  BrowserRouter,
} from "react-router-dom";

export default ()=>{
    return(
        <BrowserRouter>
            <Route>
                
            </Route>
        </BrowserRouter>
    )
}